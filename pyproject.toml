[tool.setuptools]
packages = ["melafit"]

[project]
name = "melafit"
version = "0.0.4"
requires-python = ">=3.12"
dependencies = [
    "numpy",
    "scipy",
    "pandas",
    "openpyxl",
    "matplotlib",
]

[project.urls]
Homepage = "https://github.com/vitaliy-ch25/melafit"
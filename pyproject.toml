[tool.setuptools]
packages = ["melafit"]

[project]
name = "melafit"
version = "0.0.2"
requires-python = ">=3.10"
dependencies = [
    "numpy",
    "scipy",
    "pandas",
    "openpyxl",
    "matplotlib",
]

[project.urls]
Homepage = "https://github.com/vitaliy-ch25/melafit"